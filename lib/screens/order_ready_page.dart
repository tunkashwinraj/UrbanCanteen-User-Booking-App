import 'package:flutter/material.dart';

class OrderReadyPage extends StatelessWidget {
  const OrderReadyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
